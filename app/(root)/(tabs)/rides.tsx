import { SafeAreaView, Text } from "react-native";
import React from "react";

const Rides = () => {
    return (
      <SafeAreaView>
        <Text> Rides </Text>
      </SafeAreaView>
    );
  }
  export default Rides;
